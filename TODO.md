- [ ] Inspect how “Edit Patient Info” works (modal button + JS handler)
- [x] Checked `modules/records/records.php` and `assets/js/records.js` for the toggle logic
- [x] Verified `ajax/get_patient_record.ajax.php` is JSON-only (no DOM replacement)
- [x] Implemented event-delegation click handling for `#togglePatientInfoEdit` in `assets/js/records.js`
- [ ] Manual test: open records modal → click “Edit Patient Info” → verify edit form toggles and inputs are clickable


