# TODO - Admin Audit Logs readability upgrade

- [x] Implement admin audit logs table UI and retrieval query
- [x] Implement backward-compatible action normalization in `includes/audit.php`
- [ ] (Optional later) Improve which audit callers pass what extra context into `auditLog` (entity/module/table), so actions can be more specific than generic "record" messages.


